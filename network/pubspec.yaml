name: network
description: "Module for integrating network client"
version: 0.0.1
homepage: https://github.com/AliAkberAakash/leafy

environment:
  sdk: '>=3.4.3 <4.0.0'
  flutter: ">=1.17.0"


dependencies:
  flutter:
    sdk: flutter

  dio: ^5.6.0
  get_it: 7.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  generate: true
